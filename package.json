{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "overrides": {
    "lodash": "4.18.0",
    "recharts>lodash": "4.18.0",
    "dompurify": "3.4.0",
    "jspdf>dompurify": "3.4.0",
    "jspdf-autotable>jspdf>dompurify": "3.4.0",
    "postcss": "8.5.10",
    "vite>postcss": "8.5.10",
    "fast-uri": "3.1.1",
    "@scalar/openapi-parser>fast-uri": "3.1.1",
    "orval>@scalar/openapi-parser>fast-uri": "3.1.1"
  },
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
