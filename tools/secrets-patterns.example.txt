# Example secrets patterns (DO NOT commit real secret values here)
# Replace the right-hand side with the exact secret value or a safe substring to match.
# Lines starting with # are comments.

AWS_ACCESS_KEY_ID=REDACTED
AWS_SECRET_ACCESS_KEY=REDACTED
DATABASE_URL=REDACTED_DATABASE_CONNECTION_STRING
SESSION_SECRET=REDACTED_SESSION_SECRET
# Add any other env var names, or exact strings found in commits
# e.g. 1234567890abcdef0123456789abcdef (hex tokens)
